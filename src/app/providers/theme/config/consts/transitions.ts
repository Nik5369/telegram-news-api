export const defaultTransitionDuration = 250;
const defaultTransitionEasing = 'cubic-bezier(0.4, 0, 0.2, 1)';

export const defaultTransitionSettings = `${defaultTransitionDuration}ms ${defaultTransitionEasing}`;
